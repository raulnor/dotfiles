# Personal Projects

## Diem - What day is it?

This started off as a goofy one-off because I thought day of year
was missing from UNIX [date](https://man7.org/linux/man-pages/man1/date.1.html).
Spoiler: it's `%j`. So, I wrote a ten line Objective-C program to emit `Day ##`.

I realized having a few pieces of data on my watch like, "Today is the
2nd Thursday which means I need to move my car" would be useful. Now it's an
app that I can't ship because everything is hard-coded.

I might change that someday.

## dnd.melvis.site - Mindari's Minions

This site serves as a comprehensive guide to our ongoing campaign, 
documenting everything from major historical events to local tavern 
menus. 

This is a [Hugo](https://gohugo.io/installation/) site. No theme is 
used, but [Bahunya](https://hakanalpay.com/bahunya/) and a handful 
of custom styles are used.


## dotfiles

Collection of my miscellaneous scripts and customizations. Most actual code is bash or python, but any tool is fair game.

## Google Chrome Handler

Recent issues with Safari has made me port certain links, starting with Google properties like Sheets to Google Chrome.

googlechrome:// works on iOS but not on the Mac. So I made a helper app to fill the gap. gc:// and chrome:// are also supported.

Icon is generated by running `magick app.png -modulate 100,70,83` on Chrome's app icon.

Assuming you have a swift compiler, `make && make install` should do the right thing.

## Mindari

Mindari is an Elixir / Phoenix web app where I do various random experiments. Notable items include an "On This Day" journal review tool, and a re-implementation of Saša Jurić's "The Soul of Erlang and Elixir" demo. Start with `mix phx.server` or `iex -S mix phx.server`.
Runs on [`localhost:4000`](http://localhost:4000).

## Mindari's Umbrella

Side project to host a D&D spellbook to print with chracter sheets. Elixir / Phoenix web app.

## Mindari's Word Games

Mindari's Word Games is a tool to extract Wordle, etc... scores from a shared Discord channel. I named the tool after a bard I played at a D&D oneshot. Discord's docs suggested having a cute mascot, so I kept it around.

Usage:

        mindari <command> [arguments]

The commands are:

        bot         Run discord bot for slash commands
        list        List channels with data
        help        Show this list
        monitor     Periodically monitor for posted scores
        rescan      Do a full rescan of a channel (in case of defects or edits)
        serve       Start a local webserver to show stats and a leaderboard
        stats       Print stats to standard output to use for custom graphs
        update      Scan all channels from their most recent entry forward

For those hooking into the live version, just invite the bot to your channel from the site. Commands are not needed, but will come shortly.

For those looking to self-host a private version, clone this repo and run `go build` followed by `./mindari serve`.

## M.S.I.Y.S.P.
*My Sport Is Your Sport's Punishment: A Running Tracker*

Datafeed and Phoenix web app for Strava data.

## Tempo

Phoenix web app for Apple Health data.

## tl.neocities.org

I have a personal site (tl.neocities.org). I want to flesh it out with a combination of useful technical advice, actionable political commentary and javascript (no server side, no react) toys to give people a reason to come back.

Note, this is the project I am most likely to rant incoherently and the output has the highest public visibility. I want to post more. If I appear to be spiraling or getting caught in a loop, I may need to be told to go for a run and clear my head.

